@using LDBeauty.Core.Models.Cart
@model CartDetailsViewModel

<partial name="Logo" />

<body class="bg-light">

    <div class="container">
        <main>

            <div class="row g-5 justify-content-center">
                <div class="col-md-5 col-lg-4 order-md-last">
                    @if (@Model == null)
                    {
                        <h2 class="d-flex justify-content-between align-items-center mb-3">
                            <span class="text-primary">Your cart is empty!</span>
                        </h2>
                    }
                    else
                    {
                        <h4 class="d-flex justify-content-between align-items-center mb-3">
                            <span class="text-primary">Your cart</span>
                            <span class="badge bg-primary rounded-pill">@Model.Products.Count()</span>
                        </h4>
                        <ul class="list-group mb-3">
                            @foreach (var item in @Model.Products)
                            {

                                <li class="list-group-item d-flex justify-content-between lh-sm">
                                    <div>
                                        <h6 class="my-0">@item.ProductMake @item.ProductName</h6>
                                        <small class="text-muted">Qty: @item.Quantity</small>
                                    </div>
                                    <span class="text-muted">@item.Price lv.</span>
                                </li>

                            }
                            <li class="list-group-item d-flex justify-content-between">
                                <span>Total (lv)</span>
                                <strong>@Model.TotalPrice</strong>
                            </li>
                        </ul>
                    }
                    <div class="text-center">
                        <a class="btn btn-outline-dark mt-auto" asp-action="order" asp-controller="cart" asp-route-id="@Model.CartId">Order</a>
                        </div>
                </div>
            </div>
        </main>
    </div>
</body>
